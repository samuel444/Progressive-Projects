import sqlite3
import pandas as pd

FEATURE_DATABASE_PATH = "/Users/sam/Progressive-Projects/Projects/Equity Selector/data/extensive_20260904/model_research/Macro_Features_Targets_Data.db"

connection = sqlite3.connect(FEATURE_DATABASE_PATH)
df = pd.read_sql_query(
        f'SELECT *\nFROM "Market"',
        connection,
    )

columns = list(df.columns)
features = [
    column
    for column in columns
    if not(column.startswith('^GSPC__'))
]

print(features[1:])
    